version: "3.9"
services:
    database:
        image: "mcr.microsoft.com/mssql/server"
        ports:
            - "1433:1433"
        environment:
            SA_PASSWORD: "db_password123"
            ACCEPT_EULA: "Y"
        volumes: 
          - ./Database/data:/var/opt/mssql/data